            }
        }
        sort(vowels.begin(), vowels.end());
                vowels.push_back(c);
            if (isVowel(c)) {
        for (char c : s) {
        vector<char> vowels;
    string sortVowels(string s) {
public:
class Solution {
bool isVowel(char c) {
        return string("AEIOUaeiou").find(c) != string::npos;
    }