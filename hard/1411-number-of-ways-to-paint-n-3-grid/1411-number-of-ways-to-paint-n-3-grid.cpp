class Solution {
public:
    int numOfWays(int n) {
        const int mod=1000000007;
        long long x=6,y=6;
        for(int i=2;i<=n;i++){
            long long x1=(3*x+2*y)%mod;
        }
    }
            long long y1=(2*x+2*y)%mod;
        return (x+y)%mod;
            x=x1,y=y1;
};